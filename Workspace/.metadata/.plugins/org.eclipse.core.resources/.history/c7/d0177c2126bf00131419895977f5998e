package view;

import java.io.File;
import java.util.List;

import model.GameState;
import model.PlayerStatistics;

public class OutputFileWriter extends OutputWriter {
	private File file;
	
	public OutputFileWriter(String path) {
		this.file = new File(path);
	}
	
	public OutputFileWriter(File file) {
		this.file = file;
	}
	
	@Override
	public void write(String comment, PlayerStatistics player1Statistics,
			PlayerStatistics player2Statistics)
	{
		int numberOfAllGames = player1Statistics.wonGames + player2Statistics.wonGames;
		List<GameState> anyGame = (player1Statistics != null ? player1Statistics.wonGame : player2Statistics.wonGame);
		
		// TODO implement writing to file
		System.out.println(comment);
		System.out.println("Startverteilung: " + anyGame.get(0));
		System.out.println("Gewonnene Spiele Spieler 1: " + (player1Statistics.wonGames * 100.0) / numberOfAllGames);
		System.out.println("Gewonnene Spiele Spieler 2: " + (player2Statistics.wonGames * 100.0) / numberOfAllGames);
		System.out.println("Beispiel eines von Spieler 1 gewonnenen Spiels:");
		
		if() {
			
		}
		
		System.out.println("Beispiel eines von Spieler 2 gewonnenen Spiels:");
		
	}
	
	private void printExample(int playerNumber) {
		
	}

}
